<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.dao.AuthDao">
    <select id="getMenuByRoleId" resultType="com.team.model.auth.TbAuthMenu">
		select * 
		from tb_auth_menu 
		where status=0 
		  and is_menu=1
		order by parent_id,order_num
	</select>
	
	<select id="getFunByRoleId" resultType="com.team.model.auth.TbAuthMenu">
		select * 
		from tb_auth_menu 
		where parent_id=#{parentId,jdbcType=INTEGER}
		  and is_menu=0
		order by order_num
	</select>
</mapper>
