<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.dao.TerminalChargeRecordDao">
	
	<select id="list" resultType="com.team.model.TerminalChargeRecord" parameterType="java.util.Map">
		select *
		from m_terminal_charge_record
		where 1=1
		<if test="tsid!=null">
			and tsid=#{tsid,jdbcType=INTEGER}
		</if>
		<if test="startDate!=null">
			and (createDate between #{startDate,jdbcType=TIMESTAMP} and #{endDate,jdbcType=TIMESTAMP})
		</if>
		order by createDate desc
	</select>

	<select id="count" resultType="java.util.Map" parameterType="java.util.Map">
		select round(ifnull(sum(chargeFlow),0)/1024/1024,2) chargeFlow,
			   ifnull(sum(chargeDate),0) chargeDate
		from m_terminal_charge_record
		where 1=1
		<if test="tsid!=null">
			and tsid=#{tsid,jdbcType=INTEGER}
		</if>
		<if test="startDate!=null">
			and (createDate between #{startDate,jdbcType=TIMESTAMP} and #{endDate,jdbcType=TIMESTAMP})
		</if>
	</select>
	
	<insert id="save" parameterType="com.team.model.TerminalChargeRecord">
		insert into m_terminal_charge_record
		(tsid,chargeFlow,allowFlow,chargeDate,validityDate,operator,createDate)
		values
		(
			#{tsid,jdbcType=INTEGER},
			#{chargeFlow,jdbcType=BIGINT},
			#{allowFlow,jdbcType=BIGINT},
			#{chargeDate,jdbcType=INTEGER},
			#{validityDate,jdbcType=TIMESTAMP},
			#{operator,jdbcType=VARCHAR},
			#{createDate,jdbcType=TIMESTAMP}
		)
	</insert>

</mapper>